import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  botao: {
    backgroundColor: '#fff', 
    paddingVertical: 12,
    paddingHorizontal: 25,
    borderRadius: 8,
    marginTop: 15,
    borderWidth: 1,
    borderColor: '#ddd', 
  },
  texto: {
    color: '#000', 
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
